# Cover Pool

Place your default article/category cover images here.

Supported extensions: `.jpg`, `.jpeg`, `.png`, `.webp`, `.avif`, `.gif`.

When a post has no featured image and no custom Reimu cover URL, the theme picks a deterministic image from this folder. If the folder is empty, it falls back to the configured default banner.
